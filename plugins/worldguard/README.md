# WorldGuard Docs

This folder is reserved for WorldGuard documentation content sourced from:

- https://github.com/EngineHub/WorldGuardDocs

## Status

The upstream repository could not be fetched in this environment (network access to GitHub returned a 403 tunnel error), so this folder is initialized as the first plugin docs entry point and ready for sync/import once access is available.

## Planned sync command

```bash
git clone https://github.com/EngineHub/WorldGuardDocs plugins/worldguard/upstream
```
